#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
#include <iomanip>
#include <vector>

std::vector< std::vector<float> > get_vecs_from_file(std::string file);
std::vector<float> gen_vec_from_file(std::string filename);
